@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Generar códigos de recuperación de autenticación de dos factores (2FA)"; //Generate two-factor authentication (2FA) recovery codes
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Guarde estos códigos en un lugar seguro.</strong> @*Put these codes in a safe place*@
    </p>
    <p>
        Si pierde su dispositivo y no tiene los códigos de recuperación, perderá el acceso a su cuenta.
        @* If you lose your device and don't have the recovery codes you will lose access to your account. *@
    </p>
    <p>
        La generación de nuevos códigos de recuperación no cambia las claves utilizadas en las aplicaciones de autenticación. Si desea cambiar la clave
        utilizado en una aplicación de autenticación, debe <a asp-page="./ResetAuthenticator">restablecer sus claves de autenticación..</a>
        @* Generating new recovery codes does not change the keys used in authenticator apps. If you wish to change the key
        used in an authenticator app you should <a asp-page="./ResetAuthenticator">reset your authenticator keys.</a> *@
    </p>
</div>
<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Generar códigos de recuperación</button> @*Generate Recovery Codes*@
    </form>
</div>
